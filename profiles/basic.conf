# Basic profile — low-risk hygiene. Safe for most servers.
PROFILE_NAME="basic"
PROFILE_DESCRIPTION="Updates, users audit, SSH hygiene, logging, time sync. No MFA, firewall deny, or Wazuh."
PROFILE_MODULES="updates,users,passwords,sudo,ssh,logging,time_sync,permissions"
SSH_DISABLE_PASSWORD_AUTH=false
FIREWALL_DEFAULT_DENY=false
MFA_ENABLED=false
WAZUH_ENABLED=false
SSH_HARDEN_ALGORITHMS=false
